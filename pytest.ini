[pytest]
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# For pytest-asyncio
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function

# Add source paths for pytest to find modules if needed (adjust if your project root is different for tests)
pythonpath = . 